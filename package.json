{
	"name": "practica5-infra-ian",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"coverage": "vitest run --coverage"
	},
	"devDependencies": {
		"@cloudflare/vitest-plugin": "^1.0.0",
		"@vitest/coverage-istanbul": "^4.1.11",
		"typescript": "^5.5.2",
		"vitest": "~4.1.0",
		"wrangler": "^4"
	}
}
